import reports from './js/reports.js';
import db from './js/database.js';

// We need an environment with IndexedDB to properly test database.js.
// Given this is a browser DB, we'll write a browser-based test suite that can be loaded in an HTML page.
console.log('Test file created');
